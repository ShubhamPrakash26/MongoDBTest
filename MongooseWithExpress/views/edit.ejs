<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Chat</title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>
  <div class="header">
    <h2><i class="fas fa-edit"></i> Edit Chat</h2>
    <form action="/chats" method="GET">
      <button class="action-button"><i class="fas fa-arrow-left"></i> Back</button>
    </form>
  </div>

  <div class="chat-container">
    <div class="chat-card">
      <form action="/chats/<%= chat._id %>?_method=PUT" method="POST" class="form">
        <label>From:</label>
        <input type="text" name="from" value="<%= chat.from %>" required />

        <label>To:</label>
        <input type="text" name="to" value="<%= chat.to %>" required />

        <label>Message:</label>
        <textarea name="message" rows="4" required><%= chat.message %></textarea>

        <button type="submit" class="edit"><i class="fas fa-save"></i> Update</button>
      </form>
    </div>
  </div>
</body>
</html>
